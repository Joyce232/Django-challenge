Minha participação no Django-challenge. 
Uma API feita utilizando o django rest framework e banco de dados PostgreSQL num contâiner docker

-- Set up:
   pip install requirements.txt

-- Como rodar:
   docker-compose up |
   python manage.py runserver